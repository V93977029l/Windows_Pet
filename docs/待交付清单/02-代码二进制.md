# 代码/二进制待交付

> 所有可执行代码、二进制、脚本类交付物
> ⚠️ 硬性要求：必须交付

---

## 待交付项目

| 交付物               | 文件格式 / 类型                  | 说明                        | 优先级 | 状态      |
| -------------------- | -------------------------------- | --------------------------- | ------ | --------- |
| 客户端源码           | `.gd` / `.tscn`                  | 游戏主体代码与场景文件      | 高     | 🔲 未开始 |
| 服务端源码（如需要） | 对应后端语言                     | 服务器逻辑、匹配/存档服务   | 中     | 🔲 未开始 |
| Shader               | `.gdshader`                      | 金属/冰晶/火焰史莱姆 shader | 高     | 🔲 未开始 |
| Shader 分发逻辑      | `material_manager.gd` 改造       | 按 preset 分发不同 shader   | 高     | 🔲 未开始 |
| HUD 界面逻辑         | `hud_controller.gd` + `hud.tscn` | HUD 场景与控制器对接        | 高     | 🔲 未开始 |
| 宠物行为状态机       | 新模块                           | 闲逛/打瞌睡/被摸头/落地动画 | 低     | 🔲 未开始 |
| 落地音效接线         | `drag_controller.gd`             | 落地反弹触发 SFX_LAND       | 中     | 🔲 未开始 |
| Windows 构建产物     | `.exe` + 依赖文件                | 发布版本的可执行文件        | 高     | 🔲 未开始 |
| Web 构建产物         | `index.html` + 资源              | 浏览器版本发布包            | 中     | 🔲 未开始 |

---

## Shader 分发逻辑参考

在 [material_manager.gd](file:///f:/VSCode/game/transparent-pet/modules/display/scripts/material_manager.gd) 的 `_get_shader_for_preset()` 中添加：

```gdscript
func _get_shader_for_preset(preset_id: String) -> Resource:
    match preset_id:
        "slime_1":
            return preload("res://modules/display/shaders/slime.gdshader")
        "slime_2":
            return preload("res://modules/liquid_glass/shaders/main.gdshader")
        "slime_3":
            return preload("res://modules/display/shaders/metal.gdshader")
        "slime_4":
            return preload("res://modules/display/shaders/ice.gdshader")
        "slime_5":
            return preload("res://modules/display/shaders/fire.gdshader")
        _:
            return preload("res://modules/display/shaders/slime.gdshader")
```
