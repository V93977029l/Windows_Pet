# 音频资产待交付

> 所有音效、BGM、对话音频类交付物
> 🌟 弹性要求：可瘦身或外购

---

## 待交付项目

| 资源             | 说明                | 路径                            | 优先级 | 备注                               |
| ---------------- | ------------------- | ------------------------------- | ------ | ---------------------------------- |
| `sfx_pickup.wav` | 鼠标按住宠物时触发  | `transparent-pet/assets/audio/` | 中     | 点击感音效，< 0.5s                 |
| `sfx_throw.wav`  | 抛射/甩出宠物时触发 | `transparent-pet/assets/audio/` | 中     | 短音效，< 0.5s                     |
| `sfx_land.wav`   | 落地反弹时触发      | `transparent-pet/assets/audio/` | 中     | 轻响，需在 drag_controller.gd 接线 |
| `bgm_idle.ogg`   | 待机背景音乐        | `transparent-pet/assets/audio/` | 低     | 循环，可选                         |

---

## 音频已接线项

✅ `sfx_pickup` 和 `sfx_throw` 已在 `modules/pet/scripts/pet.gd` 的 `_input()` 中通过 `EventBus.publish("play_sound", ...)` 发出；资源放入对应路径即可自动播放
