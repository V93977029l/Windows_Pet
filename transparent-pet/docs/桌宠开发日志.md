# 桌宠开发日志

## 版本管理规范

### 版本号格式

- **三位版本号**：`X.Y.Z`
  - `X`：主版本号（0-∞），1.0.0为正式版
  - `Y`：次版本号（0-∞），表示大版本更新，包含重要功能或系统变更
  - `Z`：修订号（0-∞），表示小版本更新，包含Bug修复、优化和小功能

### 版本类型标记

- **开发版本**：内部开发过程中的版本，使用`DEV`标记
  - 如：`0.0.1-DEV`、`1.0.0-DEV`（正式版的开发版本）
- **发布版本**：公开发布到平台的版本，使用`RELEASE`标记
  - 如：`0.3.0-RELEASE`（发布到Steam的预览版本）、`1.0.0-RELEASE`（正式版的发布版本）

### 记录格式

- **开发版本**：记录开发日期、开发内容
- **发布版本**：记录开发日期、发布日期、发布内容、平台信息
- **大版本更新**：每次发行版发布时，添加`## 大版本更新说明`部分，详细说明从前一个发行版到当前发行版的重要变更
- **对应提交**：仅记录功能开发、重构、修复、构建等源代码相关的提交，排除文档类（docs）提交

---

## 0.0.1-DEV - 项目启动

**开发日期**：2026 年 2 月 12 日

---

### 开发内容

- 确定开发方向为桌面宠物（桌宠）应用
- 实现基础的角色拖动功能
- 初始化透明窗口项目并实现基础拖动功能
- 添加项目文档

### 对应提交：

- `e9525ab` feat: 初始化透明桌宠项目并实现基础拖动功能
- `0f2aa55` chore: 初始化Obsidian配置文件和功能清单文档

---

## 0.0.2-DEV - 鼠标穿透功能

**开发日期**：2026 年 2 月 15 日

---

### 开发内容

- 实现桌宠的鼠标穿透功能

### 对应提交：

- `81ef052` refactor(pet): 移除未使用的右键点击脚本功能
- `451febf` feat: 实现透明窗口桌宠的拖拽功能和窗口优化
- `a5bbed8` feat(鼠标穿透): 实现鼠标穿透功能并集成到桌宠系统

---

## 0.0.3-DEV - 精确鼠标检测与黑边修复

**开发日期**：2026 年 2 月 15 日

---

### 开发内容

- 把基于范围的鼠标穿透实装为基于像素alpha通道的精确鼠标检测
- 修复了屏幕黑边的bug

### 对应提交：

- `9389e95` feat(鼠标穿透): 实现基于像素alpha通道的精确鼠标检测

---

## 0.0.4-DEV - 角色透明度设置与构建系统优化

**开发日期**：2026 年 4 月 26 日 - 2026 年 4 月 27 日

---

### 开发内容

- 添加角色透明度设置功能
- 添加GDExtension构建版本并更新VSCode配置
- 更新.gitignore并清理构建产物

### 对应提交：

- `969ac25` feat: 添加精灵透明度设置功能
- `41a7ce8` build: 添加Windows调试模板DLL和VSCode C++配置
- `e3ed98b` build: 添加GDExtension构建脚本并更新VSCode配置
- `f3d2b98` chore: 更新.gitignore并清理构建产物
- `e29ea4e` build: 更新链接库名称以使用环境变量

---

## 0.0.5-DEV - 配置文件系统与资源管理优化

**开发日期**：2026 年 4 月 27 日 - 2026 年 4 月 29 日

---

### 开发内容

- 创建配置文件系统，支持角色透明度、大小、初始位置等参数的配置化管理
- 重构资源文件结构，将SVG图标统一整理到 `assets/icons/` 目录
- 分离应用图标与游戏内贴图，便于独立管理
- 更新史莱姆角色图标样式，优化视觉效果
- 优化GDExtension构建配置和缓存处理
- 更新构建脚本并添加二进制文件到gitignore

### 对应提交：

- `edd5abb` feat(资源): 添加宠物精灵图标并更新文档
- `bd0de2e` chore: 清理并优化项目结构

---

## 0.0.6-DEV - 玻璃材质系统与Shader效果

**开发日期**：2026 年 5 月 14 日

---

### 开发内容

- 创建玻璃材质预设系统 `GlassMaterialPreset`，支持可配置的玻璃效果参数
- 实现 `slime.gdshader`：支持史莱姆角色的基础着色效果
- 重构 `PetMouseManager`，支持多种节点类型的鼠标检测（Sprit2D、Polygon2D）
- 更新角色配置系统 `PetConfig`，添加 `current_material` 配置项支持材质切换
- 简化拖动脚本 `pet_drag.gd` 的代码逻辑
- 更新场景 `node_2d.tscn`，将碰撞形状从矩形改为圆形，优化交互体验

### 对应提交：

- `70627ef` feat: 实现玻璃材质系统，添加多种预设材质与Shader效果

---

## 0.0.7-DEV - 设置系统与矢量渲染支持

**开发日期**：2026 年 5 月 15 日

---

### 开发内容

- 重构材质系统，提取 `MaterialManager` 类用于独立管理材质
- 添加设置窗口（`settings_window.tscn`）和快捷键（Ctrl+S）打开功能
- 实现中文本地化的设置界面，支持透明度、缩放、材质、置顶等配置
- 新增 `PetVectorRenderer` 类实现SVG矢量渲染支持
- 添加高清矢量缩放功能（`apply_high_res_scale` 方法）
- 优化项目渲染与抗锯齿配置
- 调整默认透明度为1.0，优化SVG导入缩放设置
- 更新磨砂玻璃着色器（`frosted_glass.gdshader`），实现更自然的视觉效果

### 对应提交：

- `e09a893` refactor: 重构材质系统，提取材质管理器类
- `d15cec1` chore: 提交Godot编辑器生成的本地缓存和配置文件
- `0bcfd83` feat: 添加设置窗口和快捷键打开功能
- `53ecbd3` feat: 添加桌宠设置窗口并优化配置功能
- `f68534b` feat: 添加矢量渲染支持与优化渲染配置
- `d8e0eaf` feat: 新增高清矢量缩放功能，调整初始透明度和SVG缩放设置

---

## 0.0.8-DEV - GDExtension构建系统升级

**开发日期**：2026 年 5 月 18 日

---

### 开发内容

- 重构并升级GDExtension编译脚本（`build.py`），支持多平台、多目标编译
- 初始化Git Submodule管理godot-cpp依赖，锁定4.3分支并启用浅克隆
- 更新`.gitignore`规则，优化仓库忽略项配置
- 调整扩展文件部署路径，将扩展文件移动到addons目录
- 简化编译成功提示信息
- 更新项目缓存与配置文件
- 移除设置窗口强制抢占焦点与独占窗口的配置

### 对应提交：

- `c4597cd` chore: 更新项目缓存与配置文件，调整窗口默认行为
- `ddab1c8` refactor(gdextension): 重构并升级GDExtension编译脚本
- `b4f3bee` style(gdextension/build): 简化编译成功提示信息
- `4bf8edc` chore: 初始化Git Submodule管理godot-cpp依赖

---

## 0.0.9-DEV - 项目结构优化

**开发日期**：2026 年 5 月 18 日

---

### 开发内容

- 移除多余的预设材质文件和全局类定义
- 删除所有Godot编辑器自动生成的缓存、配置和临时文件
- 补全SVG文件的xml声明以及svg命名空间属性，修正svg文件格式规范
- 重构项目代码结构，将所有业务代码迁移到`src/`目录下
- 按模块拆分代码到`controllers`和`managers`文件夹
- 新增`data`目录存放默认配置文件，移除旧的`config`目录
- 更新场景文件中的资源路径引用

### 对应提交：

- `ba0ad14` refactor: 移除多余的预设文件和编辑器缓存，简化项目结构
- `4563c0b` refactor(liquid-glass): 重构动态效果系统并移除透明度控制
- `d2b2bad` style(assets/svg): 补全svg文件的xml声明和命名空间
- `43e6c1d` refactor: 重构项目代码结构，整理文件目录

---

## 0.0.10-DEV - 核心管理器架构升级

**开发日期**：2026 年 5 月 18 日

---

### 开发内容

- 重命名类名并添加TP前缀，避免命名冲突（`MaterialManager`、`MousePassthroughManager`等）
- 新增窗口管理器（`WindowManager`）核心模块
- 新增拖动控制器（`DragController`）核心模块
- 新增矢量渲染器（`VectorRenderer`）核心模块
- 新增鼠标管理器（`MouseManager`）核心模块
- 重构主节点场景（`node_2d.tscn`）与主脚本，整合所有新管理器到主流程
- 删除冗余的shader uid文件
- 简化项目配置结构

### 对应提交：

- `0716a57` refactor: 重命名类名并新增核心管理器与拖动逻辑

---

## 0.0.11-DEV - 材质与窗口系统重构

**开发日期**：2026 年 5 月 20 日

---

### 开发内容

- 重构项目材质系统，采用史莱姆材质方案替代原有材质方案
- 重构窗口系统，优化窗口管理与显示逻辑
- 配置 VSCode 开发环境并更新 `.gitignore` 规则

### 对应提交：

- `fd6d5ee` refactor: 重构项目材质与窗口系统，替换为史莱姆材质方案
- `ceae294` chore: 配置vscode开发环境并更新gitignore

---

## 0.0.12-DEV - 液体玻璃演示与项目结构优化

**开发日期**：2026 年 5 月 22 日 - 2026 年 5 月 23 日

---

### 开发内容

- 实现多形状可拖拽液体玻璃演示 Demo，验证着色器交互效果
- 调整 `godot-cpp` 子模块路径，将分支切换为 `master`
- 重构项目结构，优化模块组织方式
- 调整默认材质预设与相关渲染配置
- 清理未使用的文件，更新 VSCode 文件排除规则

### 对应提交：

- `27e0bfa` refactor: 调整godot-cpp子模块路径并重构项目结构
- `2ac6ee0` feat(liquid-glass): 实现多形状可拖拽液体玻璃演示demo
- `4e78a0f` build(gitmodules): 将godot-cpp子模块分支切换为master
- `8575f46` chore: clean up unused files and update vscode exclusions
- `5e6f237` chore: 调整默认材质预设与相关配置

---

## 0.0.13-DEV - 系统托盘与任务栏隐藏功能

**开发日期**：2026 年 5 月 23 日 - 2026 年 5 月 24 日

---

### 开发内容

- 新增系统托盘图标功能，支持最小化到托盘与托盘菜单交互
- 优化鼠标穿透逻辑与窗口配置，提升桌面交互体验
- 在 GDExtension 层实现任务栏隐藏功能，桌宠不再占用任务栏空间
- 优化 GDExtension 代码逻辑，提升可维护性
- 更新 VSCode 配置，新增忽略 `*.o` 编译产物

### 对应提交：

- `aa8a6d0` feat: 新增系统托盘功能，优化鼠标穿透与窗口配置

---

## 0.1.0-RELEASE - 首个内测发行版

**开发日期**：2026 年 5 月 24 日
**发布日期**：2026 年 5 月 24 日
**发布平台**：Windows（本地内测分发，面向朋友的小范围测试）
**发布说明**：自项目启动以来历经 13 个开发迭代，首个面向朋友内测的可发行版本。

---

### 大版本更新说明

#### 一、桌宠角色

史莱姆形态的桌面宠物，以无边框透明窗口悬浮于桌面之上。

- **窗口特性**：无边框、透明背景、始终置顶（可开关）、不可调整大小、启动时自动居中
- **拖动交互**：鼠标左键按住角色即可自由拖动到桌面任意位置
- **窗口重启记忆**：关闭后重新打开，窗口自动恢复到上次的位置

#### 二、像素级鼠标穿透

当鼠标不在角色上时，窗口自动穿透所有点击事件，完全不干扰桌面正常操作。当鼠标悬停在角色上时，穿透关闭，可以正常交互。

- 基于 alpha 通道的逐像素精确检测，非矩形包围盒
- 同时支持 `Sprite2D` 像素检测和 `Polygon2D` 多边形检测两种方式
- 底层通过 GDExtension 调用 Windows `WS_EX_TRANSPARENT` / `WS_EX_LAYERED` 实现
- 任务栏图标通过 `WS_EX_TOOLWINDOW` 隐藏，桌宠不占用任务栏空间

#### 三、系统托盘

最小化到 Windows 系统托盘，常驻后台，不干扰工作。

- **左键点击托盘图标**：打开设置窗口
- **右键点击托盘图标**：弹出菜单（设置 / 退出）
- **explorer 重启恢复**：监听 `TaskbarCreated` 消息，资源管理器重启后自动重建托盘图标
- 启动时自动隐藏任务栏图标，仅保留托盘图标

#### 四、设置窗口

`` ` `` 键（反引号）或左键点击托盘图标打开设置面板，所有修改即时生效。

- **缩放控制**：滑块 + 数值输入框双向绑定，范围 0.2× ~ 4.0×，配有"确定"按钮精确应用
- **材质切换**：下拉菜单选择预设 ShaderMaterial，当前内置"可爱史莱姆"蓝色材质
- **动态效果开关**：控制史莱姆着色器的顶点波动动画（液体晃动效果），可独立开关
- **窗口置顶开关**：控制桌宠是否始终显示在所有窗口之上
- **保存配置**：将当前设置写入 `pet_config.cfg` 持久化
- **恢复默认**：一键恢复所有配置项为出厂默认值

#### 五、史莱姆着色器效果

基于 `canvas_item` 的自定义 ShaderMaterial，呈现类似玻璃/凝胶的史莱姆质感。

- **顶点动画**：基于噪声的顶点波动，模拟液体晃动（通过"动态效果"开关控制）
- **分形噪声**：4 阶 FBM（分形布朗运动）驱动片元阶段的动态扭曲
- **菲涅尔边缘光**：边缘发光效果，可配置菲涅尔强度
- **高光泽高光**：镜面反射般的高光点，增强质感
- **透明渐变**：透明度由边缘向中心递减

#### 六、矢量渲染

角色使用 SVG 矢量资源，支持任意缩放不失真。

- 资源以 SVG 文件存储（`pet_sprite.svg`），Godot 导入为 `ImageTexture`
- 缩放时对 SVG 重新渲染为更高分辨率位图（2× 超采样），避免放大模糊
- 与材质系统解耦，渲染管线独立

#### 七、项目基础信息

- **引擎**：Godot 4（渲染驱动 Direct3D 12）
- **物理引擎**：Jolt Physics
- **扩展**：GDExtension（C++），两个核心插件 —— `MousePassthrough`（鼠标穿透 + 任务栏隐藏）、`SystemTray`（系统托盘）
- **扩展编译**：Python 一键编译脚本 `build.py`，自动编译 godot-cpp + 所有扩展并部署到 `addons/`
- **外部依赖**：`godot-cpp`（Godot 官方 C++ 绑定，Git Submodule 管理，跟随 master 分支）
- **源码结构**：`src/` 目录下 8 个 GDScript 模块 —— `pet.gd`（主协调器）、`drag_controller.gd`（拖动）、`mouse_manager.gd`（鼠标检测）、`material_manager.gd`（材质管理）、`vector_renderer.gd`（SVG 矢量渲染）、`tray_manager.gd`（托盘接口）、`settings_window.gd`（设置窗口逻辑）、`pet_config.gd`（配置持久化）

#### 附：液态玻璃演示（实验性功能，未包含在内测发行包中）

独立于桌宠主程序的实验性场景（`liquid_glass_demo/`），展示多形状可拖拽液态玻璃效果。包含完整的 SDF 形状系统（圆形、超椭圆角矩形、史莱姆形）、折射/色散/菲涅尔/眩光光学模拟、双通道高斯模糊背景。此功能参考开源项目 `liquid-glass-studio` 的 WebGL 实现，作为后续效果的预研，本次内测版本不包含此演示。

---

### 开发内容

- 配置 Windows 导出预设（`export_presets.cfg`），设置产品名称、图标与纹理压缩格式
- 重构配置文件路径为 `user://` 存储，适配打包后 `res://` 只读文件系统
- 修复托盘图标在打包版本中无法加载的问题，新增多层级路径查找与兜底策略
- 适配 GDExtension 编译脚本，支持 `.a` 和 `.lib` 两种库格式自动检测
- 新增 System Tray 编辑器插件（`addons/system_tray/`），确保打包流程正确包含 GDExtension
- 优化矢量渲染器日志提示，避免打包版本误报 SVG 加载错误
- 修复场景文件中的 uid 冗余引用问题
- 添加项目版本号 `0.1.0`，更新 `.gitignore` 忽略 `export/` 导出目录
- 整合 0.0.1 至 0.0.13 共 13 个开发迭代的全部成果
- 打包 Windows 平台独立可执行程序，无需安装 Godot 引擎即可运行
- 面向朋友小范围分发，收集内测反馈

### 对应提交：

- `1f4ad8f` refactor(gdextension): 新增任务栏隐藏功能并优化代码逻辑
- `675e5d7` chore(vscode settings): 新增忽略\*.o文件
- `489de6e` feat(system_tray): 新增系统托盘编辑器插件
- `314ecb1` chore: 完成v0.1.0版本初始搭建与配置优化
- `8435728` feat(settings): add开机自启动设置功能

---

## 0.1.1-DEV - 功能完善与增强

**开发日期**：2026 年 5 月 25 日

---

### 开发内容

- 实现拖拽松手后的抛射物理效果：角色沿鼠标拖动方向飞出，受重力影响下落
- 新增弹跳机制：角色接触屏幕边缘时产生弹性碰撞反弹
- 新增地面摩擦：角色落地后逐渐减速至停止
- 新增 SVG 包围盒计算：基于 SVG 的 `width`/`height` 属性动态计算碰撞边界
- 新增抛射参数设置弹窗（`throw_settings_dialog.tscn`），支持实时调整：
  - 重力加速度（100~3000 像素/秒²）
  - 最小触发速度（控制多快才算"抛射"）
  - 最大抛射速度（速度上限）
  - 速度放大系数（拖拽速度到抛射速度的映射倍率）
  - 抛射功能开关
- 抛射参数支持持久化到配置文件，重启后保留
- 设置窗口中新增"抛射参数设置"按钮，一键打开抛射配置弹窗
- 集成液态玻璃效果到主桌宠，支持预设材质切换激活
- 重构拖拽事件处理逻辑，统一输入处理入口（`DragController`）
- 修复 sprite 与 render 的 z 轴层级同步问题
- 重构配置系统支持多史莱姆扩展，新增调试设置窗口

### 对应提交：

- `c5ebe0c` feat: 添加液态玻璃效果，重构拖拽和鼠标穿透逻辑
- `3575971` feat: 重构桌宠项目架构，新增系统托盘与配置管理功能
- `b5478d2` fix(pet.gd): 同步更新sprite和render的z轴层级
- `62bca95` refactor(pet): 重构拖拽事件处理逻辑，统一输入处理入口
- `dacdde7` refactor(settings): 重构配置系统支持多史莱姆，新增调试设置窗口
- `e5b86ef` feat: 添加抛射物理效果及参数配置功能

---

## 0.1.2-DEV - 模块化架构重构

**开发日期**：2026 年 5 月 27 日

---

### 开发内容

- 将项目从"按资源类型组织"重构为"按功能模块组织"，遵循 `rule.md` 商业级模块化架构规范
- 将 `modules/pet/` 拆分为 5 个平级模块：`pet/`、`display/`、`interaction/`、`effects/`、`settings/`
- 为每个模块创建 `api.gd` 接口契约文件，定义公共信号和方法
- 将液态玻璃特效逻辑从主控制器抽取为独立的 `LiquidGlassController`
- UI 窗口与核心模块解耦：不再直接访问 `pet_node.material_manager`、`pet_node.drag_controller` 等内部字段
- 消除所有 `api.gd` 基类文件的 `UNUSED_SIGNAL` 和 `UNUSED_PARAMETER` GDScript 警告

### 对应提交：

- `a9f333d` refactor: 完成项目模块化重构与功能完善
- `861fb9a` refactor(transparent-pet): 按功能模块重组项目目录结构
- `273fc11` refactor: 拆分模块公共API并实现基础抽象接口

---

## 0.1.3-DEV - 资源结构优化

**开发日期**：2026 年 5 月 27-28 日

---

### 开发内容

- 重构资源路径，整理项目资源结构
- 添加 liquid-glass-studio 子模块作为外部参考
- 调整项目图标路径
- 更新项目架构规范文档
- 将配置文件从 `data/` 移动到 `config/` 目录
- 将液态玻璃演示从 `liquid_glass_demo/` 移动到 `prototypes/liquid_glass/` 目录
- 更新所有文档，反映新的模块化项目结构

### 对应提交：

- `bcd8ac0` build: 添加liquid-glass-studio子模块
- `0b7f6c4` refactor: 重构资源路径，整理项目资源结构
- `43ee818` chore: 调整项目图标路径并更新规则文档
